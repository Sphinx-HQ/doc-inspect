# Changelog

## 0.1.0 (unreleased)

- Installable `watchdoc` package with `inspect()` for local PDF Info, XMP, and
  image EXIF facts.
- Thin `Client.check()` wrapping `POST /api/v1/document-checks/?wait=true`.
- Flask demo with a facts table, locked WatchDoc cards, and JSON export.
- Synthetic samples under `samples/`.
